"use strict";

var year = 2019;

try {
  alert(yea);
  console.log(decodeURIComponent("http://g%oogle.com")); //Para saber si el link esta bien...
} catch (error) {
  console.log("El error del link es...", error);
  alert(
    "a ocurrido un error en el codigo (usando  try catch... capturando error y mostrandolo)"
  );
}
